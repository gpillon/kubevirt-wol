# Agent resources are now managed by the controller
# This directory contains only the service for gRPC communication

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubevirt-wol-system

resources:
- service.yaml
images:
- name: agent
  newName: quay.io/kubevirtwol/kubevirt-wol-agent
  newTag: 20251024-160030
